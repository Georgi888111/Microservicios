server:
  port: 8081
spring:
  cloud:
    config:
      server:
        bootstrap: true
        git:
          default-label: main
          uri: https://github.com/Georgi888111/Microservicios
          search-paths: config-data
  application:
    name: config-server